# Angular NGC Template Error

When compiling with NGC in an in the view there is an undeclared variable it doesn't 
emit an error even if it's present in the `ngfactory.ts`


To test
```
npm i
npm run build
```
